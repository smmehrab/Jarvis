<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:background="@color/colorAccent"
    android:orientation="vertical"
    android:elevation="4dp"
    android:maxHeight="?android:actionBarSize"
    >

    <ImageButton
        android:layout_width="30dp"
        android:layout_height="27dp"
        android:layout_weight="1"
        android:src="@drawable/ic_arrow_back"
        android:background="@color/colorAccent"
        android:id="@+id/journal_toolbar_back_arrow" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:weightSum="6"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingVertical="5dp"
        android:paddingRight="15dp">


        <TextView
            android:id="@+id/journal_toolbar_tv"
            android:layout_width="0dp"
            android:layout_weight="4"
            android:layout_height="27dp"
            android:textStyle="bold"
            android:textSize="17sp"
            android:gravity="center_vertical"
            android:textColor="@android:color/white"
            android:text="20 November 1:48 am"/>

        <ImageButton
            android:id="@+id/journal_toolbar_calender"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:paddingLeft="20dp"
            android:layout_height="27dp"
            android:src="@drawable/ic_calander"
            android:background="@color/colorAccent"
            />
    </LinearLayout>


</androidx.appcompat.widget.Toolbar>