<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jarvis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reminder.AddReminderActivity"></activity>
        <activity android:name=".Wallet.AddWalletActivity" />
        <activity android:name=".Todo.AddTodoActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".About.AboutActivity" />
        <activity android:name=".Settings.SettingsActivity" />
        <activity android:name=".Reminder.ReminderActivity" />
        <activity android:name=".Wallet.WalletActivity" />
        <activity android:name=".Journal.JournalActivity" />
        <activity android:name=".Todo.TodoActivity" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".UserHandling.ResetPassActivity" />
        <activity android:name=".UserHandling.SignUpActivity" />
        <activity android:name=".UserHandling.SignInActivity" />
        <activity android:name=".WelcomeScreen.WelcomeActivity" />
        <activity android:name=".SplashScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>